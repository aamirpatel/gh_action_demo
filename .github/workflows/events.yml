name: First Workflow - LBG

on:
  workflow_dispatch
jobs:
 build:
   runs-on: Self-hosted
   steps:
    - name: Print
      run: echo "Build"
  
  
 test:
  needs: build
  runs-on:  Self-hosted
  steps:
    - name: Print
      run: echo "test"

 staging:
   needs: [ build, test]
   runs-on:  Self-hosted
   steps:
    - name: Print
      run: echo "staging"
 
 deploy:
   needs: [ build, test, staging]
   runs-on:  Self-hosted
   steps:
    - name: Print
      run: echo "deploy"
