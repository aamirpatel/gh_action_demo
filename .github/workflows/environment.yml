name: Environment LBG


on:
 workflow_dispatch:
 
    
jobs:
 environment-ci:
  runs-on: ubuntu-latest
  environment: dev
  steps:
   - name: Print
     run: echo "Environment is ${{vars.USER}}"
 test:
  runs-on: ubuntu-latest
  environment: test
  steps:
   - name: Print
     run: echo "Environment is ${{vars.USER}}"
 staging:
   runs-on: ubuntu-latest
   environment: staging
   steps:
   - name: Print
     run: echo "Environment is ${{vars.USER}}"
 prod:
   runs-on: ubuntu-latest
   environment: prod
   steps:
   - name: Print
     run: echo "Environment is ${{vars.USER}}"
